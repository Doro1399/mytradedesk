/**
 * MFFU Flex funded : le solde « Now » retire **50 %** du brut enregistré par payout (aligné Topstep).
 * MLL statique **100 $** après le premier payout payé/approuvé — règle produit (hors CSV).
 */
export const MFFU_FLEX_SIM_FUNDED_PAYOUT_WITHDRAWAL_SHARE = 0.5;

export const MFFU_FLEX_SIM_FUNDED_MLL_STATIC_USD_AFTER_FIRST_PAYOUT = 100;
