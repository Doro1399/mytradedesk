<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

## Journal — funded payout callouts

For `payoutCardCallout` (Progress / runway funded UIs): keep it to **two lines** — (1) plain main sentence, e.g. `You can request a payout of $X.` with **no** “according to [firm] rules”; (2) the separate dashboard-eligibility reminder. Do **not** add extra buffer explainer lines (e.g. “Payout is based on profits above your buffer.”) unless the product owner explicitly asks.
